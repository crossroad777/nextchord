# NextChord - Dependencies
# ========================

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6
starlette

# Data Models
pydantic>=2.0

# Audio Processing - MIR
madmom>=0.16.1
librosa>=0.10.0
numpy>=1.24.0
scipy>=1.10.0

# Audio Processing - Utilities
pydub>=0.25.0

# Subtitle Parsing
srt>=3.5.0

# Environment Variables
python-dotenv>=1.0.0

# Data Processing
pandas>=2.0.0

# Note Transcription (Audio to MIDI)
# basic-pitch installed separately in Dockerfile (--no-deps, TF compatibility)
onnxruntime>=1.14.0
resampy>=0.4.0

# MusicXML / MIDI / PDF
mido>=1.2.0
midiutil>=1.2.1
reportlab>=4.0.0
setuptools

# Japanese text processing
fugashi>=1.3.0
unidic-lite

# Whisper & Demucs (installed separately in Dockerfile for size control)
# openai-whisper
# demucs
